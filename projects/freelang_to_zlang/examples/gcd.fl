// Greatest Common Divisor (GCD) using Euclidean algorithm

fn gcd(a: i32, b: i32): i32 {
    if b == 0 {
        return a;
    }
    return gcd(b, a % b);
}

fn main(): i32 {
    let result: i32 = gcd(48, 18);
    println(result);
    return result;
}
