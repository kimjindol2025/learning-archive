# 팩토리얼 (재귀)

fn factorial(n) {
  if (n <= 1) {
    return 1
  } else {
    return n * factorial(n - 1)
  }
}

# 테스트
print("5! = ")
print(factorial(5))

print("10! = ")
print(factorial(10))

print("0! = ")
print(factorial(0))
